# Пример переменных окружения для вставки в ClickHouse

CH_HOST=localhost
CH_PORT=8123
CH_USER=default
CH_PASSWORD=
CH_DATABASE=default
CH_TARGET_TABLE=events_clean
CH_SELECT_SQL=SELECT id, name, created_at FROM staging.tmp_events WHERE created_at >= today()
CH_ALLOW_TYPE_CAST=true
